#include <windows.h>
#include <iostream>
#include <vector>
#include <string>
#include <boost/algorithm/string.hpp>
#include "all_diclarations.h"

using namespace std;

void Elses()
{

	if (x == "exit" || x == "esc" || x == "quit") // exit (+)
	{
		exit(1);
	}
}